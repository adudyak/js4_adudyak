{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: codeceptjs",
            "request": "launch",
            "command": "npm run codeceptjs",
            "cwd": "${workspaceFolder}"
        }
    ]
}